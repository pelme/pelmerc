My default configs, put into git for easy synchronization between multiple machines.

Install with:

git clone https://github.com/pelme/pelmerc.git ~/.pelmerc
cd .pelmerc
git submodule update --init
~/.pelmerc/bin/prc_install
