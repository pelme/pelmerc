My default configs, put into git for easy synchronization between multiple machines.

Install with:

(U="https://raw.github.com/pelme/pelmerc/master/bootstrap.sh" ; (which wget > /dev/null && wget $U -O -) || curl $U) | sh
